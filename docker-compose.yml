services:
  app:
    build: .
    container_name: pushdown_app
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    command: python app.py
